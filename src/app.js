import { generatePalette } from "./modules/utils";

const chooseColor = document.querySelector('input');

chooseColor.addEventListener("input", function (e){
    e.preventDefault;
    console.log("test");
})
a